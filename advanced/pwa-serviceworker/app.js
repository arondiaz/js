/*
Progressive web apps (PWA) y service workers

PWA: rapidas, instalables y soporte offline.

Service Workers: es la base de una PWA, son scripts que están corriendo todo el tiempo, funcionan offline, no tiene acceso al DOM, carga de forma instantanea, sincronizacion de datos.
se utiliza self en cambio de window
se utiliza caches en cambio de document
se utiliza fetch en cambio de localStorage
*/
